<nav class="navbar topnav sticky-top navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
        <div class="container-fluid">
                <a class="nav navbar-nav" href="/"><i class="icon-bitbucket"></i>Travel Bucket</a>
        </div>

        {{#if loggedIn}}
        <a href="/dashboard">MyAdventures</a>
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
</nav>